import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#3c4252',
      contrastText: '#fff',
    },
    secondary: {
      main: '#039be5',
      contrastText: '#fff',
    },
  },
});

export default theme;
